# Grafana dashboard configuration for FXML4
apiVersion: 1

providers:
  - name: "FXML4 Dashboards"
    orgId: 1
    folder: ""
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
