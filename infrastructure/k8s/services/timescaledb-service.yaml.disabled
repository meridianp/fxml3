apiVersion: v1
kind: Service
metadata:
  name: timescaledb
  namespace: fxml4
  labels:
    app: timescaledb
spec:
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: postgres
  selector:
    app: timescaledb
  type: ClusterIP
