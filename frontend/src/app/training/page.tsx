/**
 * ML Training Page
 *
 * Machine learning training studio interface
 */

import { TrainingStudio } from '@/components/ml';

export default function TrainingPage() {
  return <TrainingStudio />;
}

export const metadata = {
  title: 'ML Training Studio - FXML4',
  description: 'Train and manage machine learning models for forex trading',
};
